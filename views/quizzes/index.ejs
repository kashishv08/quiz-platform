<% layout("/layouts/boilerplate") %>

<body>
  <div class="row row-cols-lg-3 row-cols-md-2 row-cols-sm-1 g-4 mt-0">
    <% quizzes.forEach(quiz => { %>
    <a
      href="<%= currentUser && currentUser._id.toString() === quiz.owner._id.toString() ? `/examiners/${quiz._id}` : `/quizzes/${quiz._id}/attempt` %>"
      class="text-decoration-none text-dark"
    >
      <div class="card h-100 quiz-card">
        <% if (quiz.image && quiz.image.url) { %>
        <img
          src="<%= quiz.image.url %>"
          class="card-img-top"
          alt="Quiz Image"
          style="height: 15rem; object-fit: cover"
        />
        <% } %>
        <div class="card-body">
          <h5 class="card-title"><%= quiz.title %></h5>
          <p class="card-text"><%= quiz.description %></p>
        </div>
      </div>
    </a>
    <% }); %>
  </div>
</body>
